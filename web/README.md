# Vigile web portal (placeholder)

TypeScript **strict** UI (DEC-06 framework decision pending — React+Vite
recommended). Skeleton directory created in sprint 1 (ISS-001); actual
implementation starts with ISS-032.

Constraints from docs/PRODUCT_REQUIREMENTS.md §3:
- no third-party CDN / no external JavaScript,
- CSP restrictive, secure cookies,
- never writes policies directly, never holds agent keys.
